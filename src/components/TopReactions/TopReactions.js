/* eslint-disable */

import styles from "./TopReaction.module.css";

const TopReactions = ({ datas }) => {
  //   console.log(datas.map((data) => data.topReactions.map((reaction)=>({reaction.emoji} {reaction.count}))));
  //     return (
  //       <div className={styles.ReactionContainer}>
  //         {data.topReactions.map((reaction) => (
  //           <div key={reaction.id} className={styles.Reaction}>
  //             {reaction.emoji} {reaction.count}
  //           </div>
  //         ))}
  //       </div>
  //     );
};
export default TopReactions;
